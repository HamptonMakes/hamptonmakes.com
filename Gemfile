
source :rubygems

def run(command)
  puts "$> " + command
  puts `#{command}`
end

run "echo $HOSTNAME"
run "ip addr"

gem 'rack'
gem 'rack-contrib'
gem 'rack-rewrite'
gem 'sinatra'
gem 'sequel'
gem 'mysql'
gem 'haml'
gem 'builder'